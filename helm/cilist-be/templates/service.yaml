apiVersion: v1
kind: Service
metadata:
  name: {{ include "coba-cilist.fullname" . }}
  labels:
    {{- include "coba-cilist.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coba-cilist.selectorLabels" . | nindent 4 }}
