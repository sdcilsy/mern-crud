apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coba-cilist.fullname" . }}-test-connection"
  labels:
    {{- include "coba-cilist.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coba-cilist.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
